apiVersion: v1
kind: Service
metadata:
  name: e5-mistral
  namespace: {{ .Release.Namespace }}
  labels:
    app: e5-mistral
spec:
  type: ClusterIP
  selector:
    app: e5-mistral
  ports:
    - name: http
      protocol: TCP
      port: 8000
      targetPort: http
