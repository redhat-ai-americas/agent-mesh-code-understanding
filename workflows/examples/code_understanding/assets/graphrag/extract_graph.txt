-Target activity-
You are an AI assistant that helps analyze software codebases and extract dependency information.

-Repository Entity Rule (enforced throughout)-
Every document begins with a header of the form:
  "This file is located at <path> from repository url <git_repo_url>, repository slug <git_slug_value>, multi_repo <true|false>"
  The header may optionally end with: ", runtime <runtime_components>" (e.g. ", runtime Python 3.12")
Extract git_slug, git_repo, and multi_repo from this header. If ", runtime " is present in the header, also extract its value.

You MUST emit exactly ONE repository entity using the extracted values:
- entity_name MUST be exactly: the git_slug value from the document header
- entity_type: repository
- entity_description MUST begin with: "Repository hosted at <git_repo_url>." followed by a brief description of the codebase.
  If the header contains ", runtime ", include the runtime immediately after the opening sentence: "Runtime stack: <runtime_value>."
  If no runtime is present in the header, omit the "Runtime stack:" sentence entirely.
  WITH runtime:    "Repository hosted at https://github.com/acme/my-app. Runtime stack: Python 3.12. ..."
  WITHOUT runtime: "Repository hosted at https://github.com/acme/my-app. ..."

WRONG:  ("entity"<|>TIC TAC TOE SAMPLE MAIN<|>repository<|>...)   ← invented title, not the git_slug from the header
CORRECT:("entity"<|>my-app-main<|>repository<|>Repository hosted at https://github.com/acme/my-app. ...)

-Goal-
Given a text document describing a software artifact — which may be source code in any programming language, a configuration file, an infrastructure definition, or a build script — identify all software entities and the relationships between them.

-Steps-
1. Identify all entities. For each identified entity, extract the following information:
- entity_name: Most specific identifier available for the entity.
  For repositories, always use the git_slug (e.g., jhani-user-app-main).
  For all other entities: if multi_repo is true (from the document header), prefix with <git_slug>:: to
  avoid cross-repo name collisions
  (e.g., acme-payments-main::com.acme.payments.PaymentService); otherwise use the bare name.
  Within the name, preserve the original casing from the source: relative file path for modules
  (e.g., src/main/java/com/acme/PaymentService.java), fully qualified names for classes/functions
  (e.g., com.acme.PaymentService, com.acme.PaymentService.getPayment), key-path notation for config
  files (e.g., spring.datasource.url). External dependencies shared across repos are never prefixed.
- entity_type: One of the following types: [{entity_types}]
- entity_description: Comprehensive description of the entity's role, responsibilities, and key behaviors. Always refer to the entity using its exact original casing as found in the source (e.g. write "game.py" not "GAME.PY" if the source file is named game.py).
  - For all entity types except repository and dependency, the description MUST begin with: "Defined in the repository at <git_repo_url from document header>."

2. From the entities identified in step 1, identify all pairs of (source_entity, target_entity) that are *clearly related* to each other.
For each pair of related entities, extract the following information:
- source_entity: name of the source entity, as identified in step 1
- target_entity: name of the target entity, as identified in step 1
- relationship_description: explanation of how the source entity depends on or uses the target entity
- relationship_strength: a numeric score indicating strength of the relationship between the source entity and target entity
For dependencies that cross repository boundaries, use CROSS_REPO_IMPORTS or CROSS_REPO_DEPENDS_ON as the relationship type in the description.

3. For every package entity identified in step 1, emit a USES_LIBRARY relationship for each dependency entity (external library) used by modules within that package.
- source_entity: the package entity
- target_entity: the dependency entity
- relationship_description: e.g., "com.acme.payments imports org.springframework.web.bind.annotation"
- relationship_strength: a numeric score reflecting how central the dependency is to the package
Omit USES_LIBRARY if no modules within the package import or reference the dependency.

4. For every non-repository entity identified in step 1, emit a BELONGS_TO relationship linking it to its repository entity, with the following exceptions:
- Class and function entities are optional, since they are reachable through their parent module.
- Dependency entities: emit BELONGS_TO only if the dependency is internal to the repository (i.e., defined within the same codebase). Omit BELONGS_TO for external third-party libraries and frameworks.

######################
-Entity Types-
######################
- repository: A git repository. entity_name = git_slug (e.g., jhani-user-app-main).
- module: A single source file or compilation unit. entity_name = relative file path as found in the "This file is located at" annotation (e.g., src/main/java/com/acme/PaymentService.java, src/utils/my_service.py).
- class: A class definition. entity_name = fully qualified class name (e.g., com.acme.PaymentService, my_module.MyClass).
- function: A function or method. entity_name = fully qualified name including module and class where applicable (e.g., com.acme.PaymentService.getPayment, my_module.fetch_data, my_module.MyClass.my_method).
- package: A fully qualified language-specific namespace or directory that groups related modules or classes, or file path for Javascript/Typescript
- dependency: An external code reference or declared library. Covers both import statements from source files and versioned libraries declared in build or config files (with optional version, vulnerability, and deprecation metadata). Never prefixed with git_slug
- config_file: A configuration file (e.g., application.yaml, .env, terraform.tfvars)
- service: A configured service or infrastructure component (e.g., spring.datasource)
- variable: An environment variable or configuration property

######################
-Relationship Types-
######################
- IMPORTS: Module A imports Module B within the same repository
- INHERITS: Class A inherits from Class B
- CALLS: Function A calls Function B
- USES: Module A uses functionality from Module B
- DEPENDS_ON: Generic dependency relationship within the same repository
- BELONGS_TO: Module/Class/Function/Package/Config A belongs to Repository B
- USES_LIBRARY: Package A (a codebase namespace or grouping unit) uses library B
- CROSS_REPO_IMPORTS: Module A in one repo imports Module B from a different repo
- CROSS_REPO_DEPENDS_ON: Module A in one repo depends on Module B from a different repo

######################
-Output Format-
######################
One tuple per line. No preamble, no explanation, no markdown fences — tuples only.
Two tuple types:

("entity"<|><entity_name><|><entity_type><|><entity_description>)
("relationship"<|><source_entity><|><target_entity><|><relationship_description><|><relationship_strength>)

CRITICAL — Emit exact source casing. ALL CAPS only if the source identifier is ALL CAPS (e.g. `MAX_SIZE = 100`). Never introduce ALL CAPS not present in the source:

WRONG:  ("entity"<|>PLAY<|>function<|>...)              ← source says `def play()`
WRONG:  ("entity"<|>USERCONTROLLER<|>class<|>...)      ← source says `class UserController`
WRONG:  ("entity"<|>FETCHDATA<|>function<|>...)        ← source says `func fetchData()`
CORRECT:("entity"<|>play<|>function<|>...)
CORRECT:("entity"<|>UserController<|>class<|>...)
CORRECT:("entity"<|>fetchData<|>function<|>...)

CRITICAL — Use fully qualified names for module, class, and function entities. Never use bare names:

WRONG:  ("entity"<|>my_service.py<|>module<|>...)      ← bare filename; annotation says "located at src/utils/my_service.py"
WRONG:  ("entity"<|>play<|>function<|>...)             ← bare name; source is in `game.py`
WRONG:  ("entity"<|>PaymentService<|>class<|>...)      ← bare name; package is `com.acme`
WRONG:  ("entity"<|>getPayment<|>function<|>...)       ← bare name; class is `com.acme.PaymentService`
CORRECT:("entity"<|>src/utils/my_service.py<|>module<|>...)
CORRECT:("entity"<|>game.play<|>function<|>...)
CORRECT:("entity"<|>com.acme.PaymentService<|>class<|>...)
CORRECT:("entity"<|>com.acme.PaymentService.getPayment<|>function<|>...)

######################
-Example-
######################
git_slug: <extract from "repository slug" in document header>
git_repo: <extract from "repository url" in document header>
multi_repo: <extract from "multi_repo" in document header>
runtime: <value after ", runtime " in document header; leave blank if absent>
Entity_types: [repository, module, class, function, package, dependency]
Text:
"""This file is located at src/utils/my_service.py from repository url https://github.com/acme/my-app, repository slug my-app-main, multi_repo false, runtime Python 3.12"""
# my_service.py
import requests

def fetch_data(url):
    return requests.get(url).json()

Output:
("entity"<|>my-app-main<|>repository<|>Repository hosted at https://github.com/acme/my-app. Runtime stack: Python 3.12. Contains the my-app service implementation.)
("entity"<|>src/utils/my_service.py<|>module<|>Defined in the repository at https://github.com/acme/my-app. Module that provides data fetching functionality via the fetch_data function.)
("entity"<|>my_service.fetch_data<|>function<|>Defined in the repository at https://github.com/acme/my-app. Function that fetches JSON data from a given URL using the requests library.)
("entity"<|>requests<|>dependency<|>External HTTP library used by src/utils/my_service.py to perform HTTP GET requests.)
("relationship"<|>src/utils/my_service.py<|>my-app-main<|>src/utils/my_service.py BELONGS_TO the my-app-main repository<|>10)
("relationship"<|>my_service.fetch_data<|>src/utils/my_service.py<|>my_service.fetch_data is defined in src/utils/my_service.py<|>10)
("relationship"<|>src/utils/my_service.py<|>requests<|>src/utils/my_service.py IMPORTS the requests library<|>8)

######################
-Real Data-
######################
git_slug: <extract from "repository slug" in document header>
git_repo: <extract from "repository url" in document header>
multi_repo: <extract from "multi_repo" in document header>
runtime: <value after ", runtime " in document header; leave blank if absent>
Entity_types: {entity_types}
Text: {input_text}
######################
MANDATORY: Your very first output tuple MUST be the repository entity. Use git_slug, git_repo, and runtime extracted from the document header.
If runtime IS present in the header:
  ("entity"<|><git_slug><|>repository<|>Repository hosted at <git_repo>. Runtime stack: <runtime>. <brief description of what this repository contains>)
If runtime is NOT present in the header:
  ("entity"<|><git_slug><|>repository<|>Repository hosted at <git_repo>. <brief description of what this repository contains>)
Output:


