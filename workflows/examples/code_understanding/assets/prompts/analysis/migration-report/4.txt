---
title: "### Known Dependencies Requiring Upgrade"
search_mode: "local"
skip_prompt: "multi_repo"
---
Are there any dependencies that need to be upgraded and in what order?
If runtime stack version information is available (languages, frameworks, databases, middleware), include those components alongside library dependencies (e.g., JDK 1.8 → 21, Angular 15 → 17, MySQL 5.7 → 8.0).
For each, note the current version, the recommended target version, and whether the component is end-of-life.
