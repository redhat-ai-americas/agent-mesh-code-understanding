---
title: "### High-Level Summary"
---
{% if multi_repo %}
Provide a brief high-level summary of the repos in this codebase covering the
purpose, architecture, and key dependencies of each repo.
Include all the git urls you can find, including smaller repos. Treat repos with the
same git url as a single repo.
After all per-repository entries, add a short paragraph describing how the repositories relate to each other (omit if they are unrelated).
If runtime stack version information is available (languages, frameworks,
databases, middleware), include a brief "Runtime Stack" section for each repo.
Omit if not available.
{% else %}
Provide a brief high-level summary of the codebase covering its purpose, architecture, and key dependencies.
If runtime stack version information is available (languages, frameworks, databases, middleware), include a brief "Runtime Stack" section. Omit if not available.
{% endif %}

